<Hello>
    Settings)Z::Settings~~Settings are still under construction. Currently, supported commands are:<n>• Config modlog channel (mutes/bans/censoring):<n>```<n>.config modlog #your-modlog-channel```<n>• Config status channel (invites/joins/vc change):<n>```<n>.config status #your-status-channel```
    Info)I::Bot Information~~Welcome to Findbot! This is a bot with lots of experimental features.
    Commands)B::Bot Commands~~Press a reaction to get info on a specific command:
        `.color`~~Show the given color.<n>Example usage:<n>```.color 00aeef```
        `.poll`~~Start a poll.<n>• Poll title goes before the `?`<n>• Separate poll options by a comma (`,`)<n>Example usage:<n>```.poll Your favorite color? Red, Green, Blue```
        `.ping`~~Respond with "pong" and show the bot's latency.<n>Example usage:<n>```.ping```
        `.how`~~Read a tutorial! The current available tutorials are ```<n>rate<n>find<n>demo<n>nino<n>"how to beat touhou"<n>tutorial```<n>Example usage:<n>```.how rate```<n>*Tip:* Type `.how tutorial` to get instructions on how to make your own tutorial!
        `.pfp`~~Get a fellow server member's pfp.<n>Example usage:<n>```.pfp @aheze```
        `.help`~~Show this message.<n>*Advanced:* Pass in a shortcut to jump ahead.<n>Example usage:<n>```.help<n>.help sa```
        `.dance`~~Send a dance gif.<n>Example usage:<n>```.dance```
    Stats)S::Server Stats~~**Server Stats:** <n>
        <member_count> members~~Rendered chart.<a>members
        <boosters_count> server boosters~~Rendered chart.<a>boosters
        Currently_Unavailable::*App Stats are coming soon*
    Help)A::Something else?~~Join the support server!<n>https://discord.gg/MWqvUUwth7